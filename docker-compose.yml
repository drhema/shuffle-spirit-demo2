services:
  spirit-healthcare-v2:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: spirit-healthcare-v2
    ports:
      - "3001:3000"
    environment:
      - NODE_ENV=production
      - NEXT_TELEMETRY_DISABLED=1
    restart: unless-stopped
